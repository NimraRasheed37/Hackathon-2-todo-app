apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DigitalOcean Kubernetes (DOKS) specific configuration

resources:
  - ingress.yaml
  - storageclass.yaml

commonLabels:
  cloud.provider: digitalocean
